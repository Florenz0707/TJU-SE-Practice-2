# 微服务拆分剩余任务清单（按功能性划分）

更新时间：2026-03-22

## 0. 当前拆分状态（简版）

已完成并稳定运行的服务：

1. `elm-v2.0`（聚合网关 + 兼容层）
2. `elm-microservice/points-service`
3. `elm-microservice/account-service`
4. `elm-microservice/catalog-service`
5. `elm-microservice/order-service`

已完成主链路迁移：

1. 订单创建/取消/状态更新（迁移到 `order-service`）
2. 地址链路（迁移到 `order-service`）
3. 购物车链路（迁移到 `order-service`）
4. 评价链路（迁移到 `order-service`）
5. account/catalog/points 均通过内部 API 协同（RestTemplate）
6. 四服务联调 smoke 与阶段性业务对账已跑通

本文仅列“剩余任务”。

---

## 1. 订单域（Order）

目标：完成订单域迁移收口，覆盖剩余边界态与异常恢复。

### 1.1 应用服务层异常注入回归

- [ ] 在 `OrderApplicationServiceTest` 增补以下失败分支：
  - [x] 下单后调用 account 扣款失败时的错误传播与补偿行为
  - [ ] 下单后 catalog 库存预占失败时的回退行为
  - [x] 取消单时库存回补失败的可观测返回（日志/错误码）
  - [x] 订单状态推进时远端 `order-service` 返回空/异常分支

### 1.2 控制器权限边界补齐

- [ ] `OrderControllerTest` 继续补角色组合：
  - [ ] ADMIN/BUSINESS/USER 在查询、取消、状态更新上的越权边界（查询/状态/取消鉴权与基础边界已补，细分越权组合待补）
  - [ ] 非法分页参数、非法订单ID、缺失鉴权上下文

### 1.3 评价状态协同回归

- [ ] `ReviewApplicationServiceTest` 增补：
  - [ ] 删评回滚订单状态失败分支
  - [ ] 重复删评/并发删评幂等预期

---

## 2. 账户域（Account）

目标：完成 account-service 拆分后的最终收口治理。

### 2.1 跨环境配置落地验证（可选但建议）

- [ ] 在另一套本地/测试环境验证以下配置模板：
  - [ ] `ACCOUNT_SERVICE_URL`
  - [ ] `ACCOUNT_SERVICE_URL_PREVIOUS`
  - [ ] `ACCOUNT_SERVICE_ROLLBACK_URL`
  - [ ] `ACCOUNT_GRAY_MODE`
- [ ] 验证灰度切换与回滚脚本在新环境可复用

### 2.2 账户域失败演练补充

- [ ] 扩展演练样例（非生产）：
  - [ ] 钱包已扣款但订单创建失败的对账与补偿
  - [ ] 券核销成功但后续流程失败的回滚一致性

---

## 3. 目录域（Catalog）

目标：巩固库存接口契约与补偿规范，降低跨服务调用歧义。

### 3.1 库存契约文档收口

- [ ] 固化 `reserve/release` 的 `requestId/orderId` 约束
- [ ] 明确重复请求、乱序请求、超时重试的处理规范
- [ ] 与 `runbook` 对齐错误码与人工介入步骤

### 3.2 库存异常分支测试

- [ ] 增补 catalog 内部接口测试：
  - [ ] 重复预占幂等
  - [ ] 回补幂等
  - [ ] 非法 foodId/businessId 输入

---

## 4. 积分域（Points）

目标：巩固 Outbox 驱动链路，确保重试/恢复可操作。

### 4.1 Outbox 失败恢复验证

- [ ] 补一轮故障注入回归：
  - [ ] points-service 不可达时事件重试状态变化
  - [ ] 恢复后自动转 `SENT`
- [ ] 补充失败事件人工 requeue 操作记录

### 4.2 对账样例扩展

- [ ] 增加完成单、取消单、评价单的积分样本核对
- [ ] 将核对 SQL 与判定规则固化到文档

---

## 5. 鉴权与内部调用（IAM / Security）

目标：统一内部调用约束，避免“调用能通但权限不一致”。

### 5.1 内部接口鉴权一致性

- [ ] 统一核查 `X-Internal-Token` 覆盖面（调用端 + 服务端）
- [ ] 核查角色判定工具在控制器层使用一致（ADMIN/BUSINESS/USER）

### 5.2 DTO/兼容层收尾

- [ ] 继续扫描 `ResponseCompatibilityEnricher` 覆盖盲点
- [ ] 对新增内部 DTO 建立最小兼容回归

---

## 6. 联调与回归（Integration / Smoke）

目标：把“可跑通”升级为“可重复、可回归、可定位”。

### 6.1 四服务联调常态化

- [ ] 按固定节奏执行：
  - [ ] `uv run run_four_service_smoke.py --env-file .env`
  - [ ] 保留关键输出与失败上下文
- [ ] 增加失败后快速定位指引（服务日志入口、常见错误映射）

### 6.2 业务对账常态化

- [ ] 将“订单-账户-积分-Outbox”对账脚本纳入阶段回归清单
- [ ] 定义最小样本规模与通过阈值（例如近 N 笔订单）

---

## 7. 质量门禁与工程规范

目标：保持多仓并行改造期间的稳定质量基线。

### 7.1 测试覆盖

- [ ] 对新增迁移代码保持“至少同层单测 + 关键联调 smoke”
- [ ] 为高风险链路补失败分支（空返回、超时、权限拒绝）

### 7.2 代码风格与提交前检查

- [ ] 严格使用根目录 `.pre-commit-config.yaml`
- [ ] 微服务工程与单体工程统一执行 pre-commit 钩子
- [ ] 修复格式问题后再跑定向测试

---

## 8. 文档与交付物收口

目标：保证阶段结果可复现、可交接。

### 8.1 Runbook 与脚本对齐

- [ ] 逐项核对：文档命令与脚本实际参数一致
- [ ] 标注必须配置项与默认值来源（`.env` / 环境变量）

### 8.2 阶段验收记录

- [ ] 阶段5收口后生成“最终验收记录”文档：
  - [ ] 功能通过项
  - [ ] 未完成项与风险
  - [ ] 回滚路径

---

## 9. 执行顺序建议（从现在开始）

1. 先做订单域应用服务层异常注入回归（最高优先级）
2. 再做 catalog/points 失败恢复回归与文档收口
3. 然后做四服务 smoke + 业务对账常态化
4. 最后完成 account 跨环境可复用验证（可选）

---

## 10. 完成定义（DoD）

以下条件全部满足，视为本轮拆分收口完成：

1. 订单/地址/购物车/评价链路回归通过（含异常分支）
2. 四服务 smoke 稳定通过且可脚本化复现
3. 订单-账户-积分-Outbox 对账通过
4. runbook、脚本、配置模板三者一致
5. `.pre-commit-config.yaml` 规则在相关改动文件上全部通过
