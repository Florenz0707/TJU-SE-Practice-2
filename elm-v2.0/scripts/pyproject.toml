[project]
name = "elm-integration-scripts"
version = "0.1.0"
description = "Integration smoke scripts for elm-v2.0 multi-service linkage"
requires-python = ">=3.10"
dependencies = [
  "pymysql>=1.1.1",
  "python-dotenv>=1.0.1",
  "requests>=2.32.3",
]

[tool.uv]
package = false
