// NOTE: Route guarding is implemented in `src/router/index.ts`.
// This file is intentionally kept as a no-op to avoid registering duplicate
// global guards (which can cause unexpected redirects / double-next calls).
export {};
